Find the serial of every disk drive:
sudo hdparm -I /dev/sd* | grep -e Serial -e /dev/
RAID search for arrays with failed disks
sudo mdadm -D /dev/md* | grep -E '/dev/md|Failed'

History - search for *example: awk* with no repeats:
history | grep awk | sort -u
